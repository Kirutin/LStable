# Lesbian Stable Labwc keybindings

The portable public profile keeps Labwc close to its upstream defaults.

- `Super+Return`: Kitty terminal
- Additional default Labwc bindings are inherited through `<default />` in `rc.xml`.

No HyFlair zones, tiling layer, CyLab bindings or workspace overview bindings are included.
